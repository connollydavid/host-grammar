[package]
name = "host-grammar"
version = "0.1.1"
edition = "2021"
description = "Shared rules for valid agentic-host names and numbers — the grammar both the no-phase checker and the host-lifecycle generator defer to."
license = "Unlicense"

[dependencies]
